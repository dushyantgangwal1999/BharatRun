package com.example.auth.domain.util

interface Error // Marker Interface

/**
 * A marker interface in programming is an interface with no methods or
 * properties, used to "mark" or tag classes that implement it, providing
 * metadata about those classes. The purpose of a marker interface is typically
 * to indicate that the class possesses some quality or
 * should be treated in a specific way by other parts of the code.
 */